// Optional single-MP3 backend. Deploy cloudflare-worker/ (see README), then paste
// your worker URL here, e.g. "https://korean-single-mp3.<account>.workers.dev".
// When empty, the app falls back to multi-part Google TTS download links.
export const SINGLE_MP3_WORKER_URL = '';
